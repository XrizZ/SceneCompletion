#include "registry.hpp"

#include "distance.hpp"

namespace imdb
{
/*
    bool register_distance_functions()
    {
        static distfn_map_t distfn_map;

        distfn_map["l1norm"] = l1norm<vec_f32_t, float>();
        distfn_map["l2norm"] = l2norm<vec_f32_t, float>();
        distfn_map["jsd"] = jsd<vec_f32_t, float>();

        registry().get<distfn_map_t>("distance_functions") = distfn_map;

        return true;
    }

    bool distance_function_registered = register_distance_functions();
*/
}
